# VT Panel - Веб-панель управления WhatsApp сессиями

Веб-панель для управления автоматизацией вступления в WhatsApp и Telegram группы.

## 🚀 Быстрый старт

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте переменные окружения:**
   Создайте файл `.env` с содержимым:
   ```
   BOT_TOKEN=ваш_токен_telegram_бота
   WA_PHONE=ваш_номер_телефона_для_пэйринг_кода (опционально)
   WEB_PORT=3000 (опционально, по умолчанию 3000)
   ```

3. **Запустите сервер:**
   ```bash
   node vt.js
   ```

4. **Откройте веб-панель:**
   Откройте в браузере: `http://localhost:3000`

## 📋 Функции веб-панели

### Управление сессиями
- ✅ Просмотр всех сессий с их статусами
- ✅ Создание новых сессий
- ✅ Активация сессий через QR код или пэйринг-код
- ✅ Генерация нового QR кода
- ✅ Просмотр групп для каждой сессии
- ✅ Настройка интервалов между вступлениями
- ✅ Удаление сессий

### Мониторинг
- ✅ Статистика: общее количество сессий, активных, ссылок в очереди
- ✅ Real-time обновления статусов через WebSocket
- ✅ Просмотр сохраненных ссылок
- ✅ Просмотр пользователей бота

### Автоматизация
- ✅ Автоматическое отслеживание ссылок в группах WhatsApp
- ✅ Автоматическое вступление в группы с настраиваемыми интервалами
- ✅ Очередь вступлений с уведомлениями

## 🎨 Интерфейс

Веб-панель имеет современный и удобный интерфейс с:
- Адаптивным дизайном
- Real-time обновлениями
- Интуитивным управлением
- Красивыми карточками сессий
- Модальными окнами для действий

## 🔧 API Endpoints

### Сессии
- `GET /api/sessions` - Получить список всех сессий
- `GET /api/sessions/:name` - Получить информацию о сессии
- `POST /api/sessions` - Создать новую сессию
- `POST /api/sessions/:name/activate` - Активировать сессию
- `POST /api/sessions/:name/newqr` - Сгенерировать новый QR код
- `GET /api/sessions/:name/groups` - Получить список групп
- `PUT /api/sessions/:name/intervals` - Обновить интервалы
- `DELETE /api/sessions/:name` - Удалить сессию

### Ссылки и пользователи
- `GET /api/links?page=1` - Получить сохраненные ссылки
- `GET /api/users` - Получить список пользователей

## 📡 WebSocket события

- `session_status` - Обновление статуса сессии
- `session_update` - Обновление данных сессии
- `session_deleted` - Удаление сессии
- `qr_code` - Получение QR кода для сканирования
- `pairing_code` - Получение пэйринг-кода

## 🔐 Безопасность

⚠️ **Важно:** Использование автоматизации может нарушать правила WhatsApp и Telegram. Используйте на свой риск и соблюдайте лимиты платформ.

## 📝 Примечания

- Веб-панель работает параллельно с Telegram ботом
- Все функции доступны как через веб, так и через Telegram
- QR коды отображаются в реальном времени через WebSocket
- Статусы обновляются автоматически каждые 5 секунд

## 🛠️ Технологии

- **Backend:** Node.js, Express, Socket.io
- **Frontend:** Vanilla JavaScript, HTML5, CSS3
- **WhatsApp:** @whiskeysockets/baileys
- **Telegram:** Telegraf

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота в `.env`
2. Доступность порта 3000
3. Логи в консоли на наличие ошибок

