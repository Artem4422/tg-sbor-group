# TG Panel - Веб-панель управления Telegram userbot-сессиями

Веб-панель для управления автоматизацией вступления в Telegram группы с помощью аккаунтов Telegram (userbot / MTProto).

## 🚀 Быстрый старт

1. **Установите зависимости:**

```bash
cd tg_project
npm install
```

2. **Настройте переменные окружения:**

Создайте файл `.env` в папке `tg_project` со следующим содержимым:

```env
TG_API_ID=ваш_api_id_telegram
TG_API_HASH=ваш_api_hash_telegram
TG_WEB_PORT=4000          # опционально, по умолчанию 4000
```

`TG_API_ID` и `TG_API_HASH` можно получить в my.telegram.org (раздел API Development Tools).

3. **Запустите сервер:**

```bash
node tg.js
```

4. **Откройте веб-панель:**

Откройте в браузере: `http://localhost:4000`

## 📋 Функции веб-панели (минимальный первый вариант)

- ✅ Просмотр всех Telegram-сессий с их статусами
- ✅ Создание новой сессии (вывод авторизационного кода в консоль / логах)
- ✅ Активация сессии по коду авторизации (через REST API)
- ✅ Хранение сессий на диске (`tg_sessions/<sessionName>.session`)
- ✅ Автовступление в Telegram-группы по ссылкам `https://t.me/...` через REST API (очередь вступлений)

Дальше можно расширять:

- 🔄 Настройка интервалов между вступлениями
- 👀 Автоматический мониторинг ссылок в диалогах
- 🤖 Управление через отдельного Telegram-бота

## 🔧 API Endpoints (первый набор)

Все пути начинаются с `/api/tg`.

### Сессии

- `GET /api/tg/sessions` — список всех сессий
- `POST /api/tg/sessions` — создать новую сессию
  - body: `{ "name": "имя_сессии", "phone": "+79991234567" }`
  - возвращает статус и информацию, куда отправлен код
- `POST /api/tg/sessions/:name/confirm` — подтвердить код авторизации
  - body: `{ "code": "12345" }`
- `GET /api/tg/sessions/:name` — информация о сессии
- `DELETE /api/tg/sessions/:name` — удалить сессию

### Вступление в группы

- `POST /api/tg/sessions/:name/join` — добавить задачу на вступление в группу
  - body: `{ "link": "https://t.me/..." }`

## 📡 WebSocket события

- `tg_session_status` — обновление статуса Telegram-сессий
- `tg_join_queue_update` — обновление состояния очереди вступлений

## 🛠️ Технологии

- **Backend:** Node.js, Express, Socket.io
- **Telegram userbot:** gramJS (`telegram` npm-пакет, MTProto)

## ⚠️ Важно

Использование userbot-аккаунтов может нарушать правила Telegram.  
Используйте на свой страх и риск, соблюдайте лимиты и не спамьте.


